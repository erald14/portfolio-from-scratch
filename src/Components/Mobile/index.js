import React from 'react';
import {style} from './index.scss';

const Mobile = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Mobile.propTypes = {

};

export default Mobile;
