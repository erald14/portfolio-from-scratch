import React from 'react';
import {style} from './index.scss';

const Backend = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Backend.propTypes = {

};

export default Backend;
