import React from 'react';
import {style} from './index.scss';

const Shadow = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Shadow.propTypes = {

};

export default Shadow;
