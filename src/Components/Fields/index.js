import React from 'react';
import {style} from './index.scss';

const Fields = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Fields.propTypes = {

};

export default Fields;
